{{> site-sidebar}}


<ul class="products">
    {{#each products}}
    <li>
        <a href="#">
            <img src="{{product_image}}">
            <p>{{name}}</p>
            <p>{{price}}TL</p>
        </a>
    </li>
    {{/each}}
</ul>
